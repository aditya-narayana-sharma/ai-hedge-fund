# Base URL of the AI Hedge Fund backend.
# Defaults to http://localhost:8000 when unset (see src/services/api.ts).
# Vite only exposes variables prefixed with VITE_ to the browser bundle.
VITE_API_URL=http://localhost:8000
